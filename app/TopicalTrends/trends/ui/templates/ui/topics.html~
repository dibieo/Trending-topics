<head>
        {% load static %}
        <LINK href="{% get_static_prefix %}general.css" rel="stylesheet" type="text/css">
</head>

  {% if freqTopics %}
                   {% for topic in freqTopics %}
                        <div>
							<a href='http://127.0.0.1:8000/ui/?input={{ topic.0 }}'>{{topic}}</a>
                            <hr>
                        </div>
                    {% endfor %}
		    </div>
                {% else %}
                    <p><strong>No hot trends to show</strong></p>
                {% endif %}
                
